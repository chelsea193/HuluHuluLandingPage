node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Large pristine media originals (kept locally; web-optimized copies live in public/).
# Deliberately outside public/ — Vite copies all of publicDir into dist.
asset-originals/
